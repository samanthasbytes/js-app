// Create a new variable called pokemonList and assign to it a blank array. This array contains Pokémon data to display in your application.

// Add 3 Pokémon objects to the pokemonList array. Each object represents one Pokémon and needs to have the same keys to avoid errors.

/* Add the following three keys to each Pokémon object and make up a value for each of them
  name (string, e.g., "Bulbasaur")
  height (number, e.g., 7)
  types (array of strings, e.g., ['grass', 'poison'])
  
  You can use Pokedex.org as a reference.
*/

let pokemonList = ["Charizard", "Jigglypuff", "Psyduck"];

let Charizard = {
  name: "Charizard",
  height: 1.7,
  types: ["fire", "flying"],
};

let Jigglypuff = {
  name: "Jigglypuff",
  height: 0.5,
  types: ["fairy", "normal"],
};

let Psyduck = {
  name: "Psyduck",
  height: 0.8,
  types: "water",
};
